# Overrides demo

Combine a deep static override with a deterministic callback override while
FixtureSmith generates every unrelated required field.

```sh
pnpm --filter @fixturesmith-example/overrides check
```
