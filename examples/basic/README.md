# Basic FixtureSmith demo

Generate a typed fixture and a deterministic collection from a Zod schema.

```sh
pnpm --filter @fixturesmith-example/basic check
```

Use the repository's **Open in StackBlitz** link to run this example in a browser.
