# Scenarios demo

Compose two fixture definitions into one coherent application state, replay the
whole state from a seed, and override only the field a test cares about.

```sh
pnpm --filter @fixturesmith-example/scenarios check
```
